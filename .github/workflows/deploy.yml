name: Continuous Deployment

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to Server
        run: |
          echo "Deploying CircuitGPT to production server..."
          echo "Add SSH deploy steps here when production server is configured."
